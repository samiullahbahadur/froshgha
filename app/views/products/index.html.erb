

<%@product.each do |product|%>
    
    <h1> Categories Name:   <%=  category.category_name%></h1>
    <%= link_to 'Edit', edit_category_path(category) %>
    <%= button_to 'Remove', category_path(category), method: :delete %>

<%end%>

